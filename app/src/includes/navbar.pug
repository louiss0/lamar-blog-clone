include ../mixins/container.pug
include ../mixins/bars-icon.pug



- const links = ["home", "about", "posts", "contact"]
    
nav.text-gray-50.bg-gray-900.min-h-24.relative
    
    
    +container(
        "p-4 bg-inherit h-24 z-20 inset-0 flex justify-between items-center gap-8 relative"
        )(w:lg="w-10/12")
        
        a.capitalize(href="/")
            .text-2xl lamar
        
        ul.hidden(w:lg="flex gap-8 flex-1 items-center") 
            
            for link in links

            

                if link === 'home'
                
                    li.p-2 

                        a(
                            href="/"
                            class="capitalize" 
                            w:hover="text-gray-400" 
                        ) #{link}
                else 

                    li.p-2 
                
                        a(
                            href=`/src/pages/${link}.pug` 
                            w:hover="text-gray-400" 
                        ).capitalize #{link}
    
        button#nav-dropdown-toggler(
            class="bg-gray-500 h-16 grid w-16 place-items-center"
            w:lg="hidden"
        )

            .w-6.h-6
                +barsIcon
        
    ul#nav-dropdown(
        class="bg-inherit w-full transform -translate-y-full left-0 z-10 absolute" 
        ) 
            
            for link in links

            
                if link === 'home'
                
                    li.h-20.p-2(class="grid place-items-center") 

                        a(
                            href="/" 
                            class="capitalize"
                        ) #{link === 'posts'? 'all posts': link }
                else 

                    li.h-20.p-2(
                        class="grid text-gray-400 place-items-center"
                        w:hover="text-gray-200" 
                        ) 
                
                        a(
                            href=`/src/pages/${link}.pug` 
                            class="capitalize"
                        ) #{link === 'posts'? 'all posts': link }




                        